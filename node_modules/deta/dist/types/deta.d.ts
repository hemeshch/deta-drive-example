import BaseClass from './base';
import DriveClass from './drive';
export default class Deta {
    private projectKey;
    constructor(projectKey: string);
    Base(baseName: string): BaseClass;
    Drive(driveName: string): DriveClass;
}
